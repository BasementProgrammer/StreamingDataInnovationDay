version: 1
applications:
- appRoot: Hugo
  frontend:
    phases:
      preBuild:
        commands:
          # Add any prebuild commands necessary here.
      build:
        commands:
          - hugo
    artifacts:
      baseDirectory: public
      files:
        - '**/*'
    cache:
      paths: []
